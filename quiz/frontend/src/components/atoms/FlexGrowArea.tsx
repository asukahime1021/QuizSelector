import Area from './Area';
import styled from 'styled-components';

const FlexGrowArea = styled(Area)`
    flex-grow: 1;
    display:flex;
    align-items: flex-start;
`

export default FlexGrowArea;
